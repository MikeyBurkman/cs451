{
  "name": "cs-451-code",
  "description": "Code samples for CS 451",
  "version": "0.0.1",
  "author": "MikeyBurkman",
  "license": "MIT",
  "private": true,
  "engines": {
    "node": ">=4"
  },
  "scripts": {
    "clean": "del './app/**/*.js*'",
    "compile": "npm run clean && tsc --pretty",
    "ts": "ts-node"
  },
  "dependencies": {
    "del-cli": "^0.2.0",
    "ts-node": "^1.3.0",
    "typescript": "^v2.0.3"
  }
}
